package com.raritan.tdz.ticket.home;

public interface TicketResetBehavior {

	/**
	 * reset the ticket fields using the original ticket data
	 * @param target
	 */
	public void reset(Object target);
	
}
